<resources>

    <!-- 기본 테마: NoActionBar로 상단바 제거 -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:selectableItemBackgroundBorderless">@android:attr/selectableItemBackgroundBorderless</item>
    </style>

    <!-- AddItemActivity를 위한 Dialog 스타일 추가 -->
    <style name="AppTheme.Dialog" parent="Theme.AppCompat.Dialog">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:windowCloseOnTouchOutside">true</item>
    </style>

    <!-- Tab Layout 텍스트 스타일 -->
    <style name="TabTextStyle">
        <item name="android:textSize">16sp</item>
        <item name="android:textColor">#000000</item>
        <item name="android:textStyle">bold</item>
    </style>

    <!-- Calendar DatePicker Custom Style -->
    <style name="CustomDatePicker" parent="Theme.AppCompat.DayNight.Dialog">
        <item name="android:calendarTextColor">#65B120</item>
        <item name="android:headerBackground">#65B120</item>
        <!-- Removed android:daySelectorColor as it is not allowed -->
    </style>

    <!-- Calendar Header Text Appearance -->
    <style name="CalendarHeaderText">
        <item name="android:textSize">16sp</item>
        <item name="android:textColor">@color/colorAccent</item>
        <item name="android:textStyle">bold</item>
    </style>

    <!-- 공통 버튼 스타일 -->
    <style name="CustomButtonStyle">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_weight">1</item>
        <item name="android:padding">10dp</item>
        <item name="android:textColor">@android:color/black</item>
        <item name="android:textSize">16sp</item>
    </style>


</resources>
